local P,R,U,C=game:GetService("Players"),game:GetService("RunService"),game:GetService("UserInputService"),workspace.CurrentCamera local L,es,ai,sp,nc,fl,cn=P.LocalPlayer,false,false,false,false,false,{} local sg=Instance.new("ScreenGui",L.PlayerGui) sg.ResetOnSpawn=false local f=Instance.new("Frame",sg) f.Size,f.Position,f.BackgroundColor3,f.Active,f.Draggable=UDim2.new(0,260,0,270),UDim2.new(0.5,-130,0.3,-135),Color3.fromRGB(25,25,25),true,true local mF=Instance.new("Frame",f) mF.Size,mF.Position,mF.BackgroundTransparency=UDim2.new(1,0,1,-30),UDim2.new(0,0,0,30),true local function btn(t,y) local b=Instance.new("TextButton",mF) b.Size,b.Position,b.BackgroundColor3,b.Text,b.TextColor3,b.Font,b.TextSize=UDim2.new(0,220,0,35),UDim2.new(0,20,0,y),Color3.fromRGB(50,50,50),t,Color3.fromRGB(255,100,100),Enum.Font.SourceSansBold,14 return b end local eB,aB,sB,nB,fB=btn("ESP + HP: OFF",10),btn("Lock-On: OFF",50),btn("Speed/Jump: OFF",90),btn("Noclip: OFF",130),btn("Fly Mode: OFF",170) local tH=Instance.new("TextLabel",f) tH.Size,tH.BackgroundColor3,tH.Text,tH.TextColor3,tH.Font,tH.TextSize=UDim2.new(1,-30,0,30),Color3.fromRGB(35,35,35),"  zxtata | MENU",Color3.fromRGB(255,255,255),Enum.Font.SourceSansBold,14 local mB=Instance.new("TextButton",f) mB.Size,mB.Position,mB.BackgroundColor3,mB.Text,mB.TextColor3=UDim2.new(0,30,0,30),UDim2.new(1,-30,0,0),Color3.fromRGB(45,45,45),"-",Color3.fromRGB(255,255,255) local min=false mB.MouseButton1Click:Connect(function() min=not min mF.Visible=not min f.Size=min and UDim2.new(0,260,0,30) or UDim2.new(0,260,0,270) mB.Text=min and "+" or "-" end) U.InputBegan:Connect(function(i,g) if not g and i.KeyCode==Enum.KeyCode.Insert then f.Visible=not f.Visible end end) local function add(c) if not es or c==L.Character then return end local r,h=c:WaitForChild("HumanoidRootPart",5),c:WaitForChild("Humanoid",5) if not r or not h or c:FindFirstChild("S_ESP") then return end local b=Instance.new("BoxHandleAdornment",c) b.Name,b.Size,b.Color3,b.AlwaysOnTop,b.ZIndex,b.Adornee="S_ESP",c:GetExtentsSize()+Vector3.new(0.5,0.5,0.5),Color3.fromRGB(0,255,255),true,10,r local g=Instance.new("BillboardGui",c) g.Name,g.Size,g.AlwaysOnTop,g.ExtentsOffset,g.Adornee="S_UI",UDim2.new(0,100,0,30),true,Vector3.new(0,3,0),r local tl=Instance.new("TextLabel",g) tl.Size,tl.BackgroundTransparency,tl.TextColor3,tl.Font,tl.TextSize=UDim2.new(1,0,1,0),true,Color3.fromRGB(255,255,255),Enum.Font.SourceSansBold,12 R.RenderStepped:Connect(function() if c and h and tl then tl.Text=c.Name.." ["..math.floor(h.Health).."]" end end) end eB.MouseButton1Click:Connect(function() es=not es eB.Text,eB.TextColor3=es and "ESP + HP: ON" or "ESP + HP: OFF",es and Color3.fromRGB(100,255,100) or Color3.fromRGB(255,100,100) if es then for _,p in ipairs(P:GetPlayers()) do if p.Character then add(p.Character) end end table.insert(cn,P.PlayerAdded:Connect(function(p) p.CharacterAdded:Connect(add) end)) else for _,o in ipairs(cn) do o:Disconnect() end cn={} for _,p in ipairs(P:GetPlayers()) do if p.Character then if p.Character:FindFirstChild("S_ESP") then p.Character.S_ESP:Destroy() end if p.Character:FindFirstChild("S_UI") then p.Character.S_UI:Destroy() end end end end end) aB.MouseButton1Click:Connect(function() ai=not ai aB.Text,aB.TextColor3=ai and "Lock-On: ON" or "Lock-On: OFF",ai and Color3.fromRGB(100,255,100) or Color3.fromRGB(255,100,100) end) sB.MouseButton1Click:Connect(function() sp=not sp sB.Text,sB.TextColor3=sp and "Speed/Jump: ON" or "Speed/Jump: OFF",sp and Color3.fromRGB(100,255,100) or Color3.fromRGB(255,100,100) local h=L.Character and L.Character:FindFirstChild("Humanoid") if h then h.WalkSpeed,h.JumpPower=sp and 50 or 16,sp and 100 or 50 end end) nB.MouseButton1Click:Connect(function() nc=not nc nB.Text,nB.TextColor3=nc and "Noclip: ON" or "Noclip: OFF",nc and Color3.fromRGB(100,255,100) or Color3.fromRGB(255,100,100) end) R.Stepped:Connect(function() if nc and L.Character then for _,v in ipairs(L.Character:GetDescendants()) do if v:IsA("BasePart") then v.CanCollide=false end end end end) fB.MouseButton1Click:Connect(function() fl=not fl fB.Text,fB.TextColor3=fl and "Fly Mode: ON" or "Fly Mode: OFF",fl and Color3.fromRGB(100,255,100) or Color3.fromRGB(255,100,100) local r=L.Character and L.Character:FindFirstChild("HumanoidRootPart") if r then if fl then local bv,bg=Instance.new("BodyVelocity",r),Instance.new("BodyGyro",r) bv.Name,bv.MaxForce,bv.Velocity="F_Vel",Vector3.new(1e5,1e5,1e5),Vector3.new(0,0,0) bg.Name,bg.MaxTorque,bg.CFrame="F_Gyr",Vector3.new(1e5,1e5,1e5),C.CFrame else if r:FindFirstChild("F_Vel") then r.F_Vel:Destroy() end if r:FindFirstChild("F_Gyr") then r.F_Gyr:Destroy() end end end end) R.RenderStepped:Connect(function() if ai then local t,s=nil,math.huge local m=L:GetMouse() for _,p in ipairs(P:GetPlayers()) do if p~=L and p.Character and p.Character:FindFirstChild("Head") then local v,on=C:WorldToViewportPoint(p.Character.Head.Position) if on then local d=(Vector2.new(m.X,m.Y)-Vector2.new(v.X,v.Y)).Magnitude if d<s then s=d t=p end end end end if t then C.CFrame=CFrame.new(C.CFrame.Position,t.Character.Head.Position) end end if fl and L.Character and L.Character:FindFirstChild("HumanoidRootPart") then local r=L.Character.HumanoidRootPart local bv,bg=r:FindFirstChild("F_Vel"),r:FindFirstChild("F_Gyr") if bv and bg then bg.CFrame=C.CFrame local dir=Vector3.new(0,0,0) if U:IsKeyDown(Enum.KeyCode.W) then dir=dir+C.CFrame.LookVector end if U:IsKeyDown(Enum.KeyCode.S) then dir=dir-C.CFrame.LookVector end if U:IsKeyDown(Enum.KeyCode.A) then dir=dir-C.CFrame.RightVector end if U:IsKeyDown(Enum.KeyCode.D) then dir=dir+C.CFrame.RightVector end bv.Velocity=dir~=Vector3.new(0,0,0) and dir.Unit*50 or Vector3.new(0,0,0) end end end)
